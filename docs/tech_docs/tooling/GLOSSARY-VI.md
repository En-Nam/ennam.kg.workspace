# Vietnamese glossary (all VI docs, both products)

One term per concept across all 8 Vietnamese documents. Engineering terms Vietnamese developers
normally say in English stay in English; general-language concepts are translated.

| English | Vietnamese (use exactly) | Notes |
|---|---|---|
| self-hosted | tự host | never "tự vận hành" (reads as autonomous) or "tự triển khai" |
| production-like | gần giống production | never "tương đương production" |
| pre-production | giai đoạn tiền production | |
| implemented / wired / exercised (status defs) | đã hiện thực · đã tích hợp · đã chạy với dữ liệu | same wording in every doc's status vocabulary |
| knowledge graph / graph | knowledge graph / graph | not "đồ thị (tri thức)" in prose; diagrams may shorten |
| node / edge | node / cạnh | |
| tool (MCP tool, agent tool) | tool | "53 tool MCP hoạt động" |
| server (API server, Go server) | server | not "máy chủ" |
| model (LLM model) | mô hình | "mô hình cục bộ"; product/model names stay English |
| local | cục bộ | |
| cloud | cloud | |
| session (auth / agent / conversation) | phiên | |
| section (document section) | mục | |
| project | dự án | |
| ingest / ingestion | nạp / nạp dữ liệu | |
| hybrid search | tìm kiếm lai | |
| fusion (RRF) | trộn kết quả (RRF) | not "hợp nhất" |
| entity resolution | hợp nhất thực thể | |
| merge (aliases) | gộp | |
| deterministic | tất định | |
| whitelist / allow-list | whitelist | |
| query plan / plan cache | query plan / cache query plan | |
| audit / audit log | audit / nhật ký audit | not "kiểm toán" |
| secret | secret | |
| credential | thông tin xác thực | |
| guard (reliability/safety guard) | guard | not "chốt chặn"; write-claim guard = "guard chống báo ghi sai" |
| digest | bản tóm lược (digest) | first use with English in brackets, then "bản tóm lược" |
| fail soft / fail loud | suy giảm êm / báo lỗi rõ ràng | |
| rate limiting | giới hạn tốc độ (rate limiting) | |
| hands-free (voice) | không cần chạm tay | not "rảnh tay" |
| badge (UI) | nhãn | not "huy hiệu" |
| facts (memory kind) | dữ kiện | "sự kiện" is reserved for the `event` entity type |
| progress stream | luồng tiến độ | not "tiến trình" (= process) |
| 3D graph explorer | trình khám phá graph 3D | not "trình duyệt" (= browser) |
| enforce | thực thi / áp dụng | not "cưỡng chế" |
| source DB engine | loại CSDL nguồn | |
| host (machine) | máy chủ vật lý / host | only for physical host machines |
