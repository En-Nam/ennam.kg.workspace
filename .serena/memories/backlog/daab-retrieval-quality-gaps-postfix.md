# Backlog — DAAB retrieval quality gaps (post-FR-001-fix, evidence-ranked)

**Source:** sim-consumer RE-RUN 2026-07-13 on Cảng Định An (`592c7ff7…`) after this week's fixes went live. Full report: `other_projects/daab-sim-consumer/findings-rerun.md`. These are evidence-driven hardening items (a controllable consumer hit real defects), NOT speculative features.

## FR-001 is VALIDATED — no new retrieval feature needed
Tier-3 cross-doc went 0.5 → **1.25 (consumer-as-shipped) / 1.75 (server-capability)**. `kg_graph_retrieve` now seeds (10) + expands (56–81 chunks). Flat-vs-graph control CONFIRMED graph expansion surfaces an edge-only doc (chủ trương đầu tư) that flat search missed at equal budget → a real categorical win, not just convenience. FR-001/004 done as a design. Residual shortfall is NOT a missing feature — it's the ranked gaps below.

## Ranked gaps (evidence-backed)
1. **[RESOLVED 2026-07-13] Document duplication.** Fixed via `docs/superpowers/plans/2026-07-13-daab-document-dedup.md`, executed end-to-end with superpowers:subagent-driven-development (checkpoint `mem:checkpoint/subagent-driven-dedup-2026-07-13`). Added tier-3 global per-project content-hash dedup to the ingest pipeline (Go `FindByContentHash` store+handler, Python `KGClient` method + engine wiring, all TDD'd and task-reviewed clean) — proven E2E on the live stack (identical re-upload correctly reuses the existing node instead of creating a duplicate). Cleaned up Cảng Định An (592c7ff7-...) and re-ingested: was 145 docs/74 distinct hashes, now **77 docs = 77 distinct hashes** (invariant holds, 0 dup_doc_similar_edges, 0 failed drafts). Original evidence retained below for context.

   Original finding: VERIFIED in DB: Cảng project has **145 document nodes but only 75 distinct titles → 70 dups (48%)** (e.g. "2021 QĐ 1579-TTg" ×4, "Hợp đồng hợp tác" ×3). Duplicate copies link at `edge_similarity:1.0`, so `kg_graph_retrieve` top seeds are dominated by duplicate pairs → false corroboration + wasted result slots. Root cause was ingestion dedup keying on `(source_type, source_id, content_hash)`; the same PDF re-uploaded got a new source_id → cross-upload duplicates escaped.
2. **[RESOLVED 2026-07-13] Stale bridge binary.** Verified fixed: running `daab-bridge` container AND the local stdio `ennam.kg.go/bin/kg-bridge` (rebuilt Jul 13, what sim-consumer's `.mcp.json` invokes) both now expose `kg_related_documents` + `kg_document_shared_entities` (strings-confirmed). `bin/kg-bridge` is now **gitignored** — so instead of shipping a committed binary, consumers build from source (`make build`), which is the correct release-hygiene outcome (no stale binary in git). Original finding retained below.

   Original finding: Committed `bin/kg-bridge` was **Jun 25**, predating the two tools (added 2026-06-30), so no MCP consumer could call them; the re-run had to curl the HTTP routes. Server capability works (shared_count / max_idf / top_shared_entity — Q12's need).
3. **[RESOLVED 2026-07-13] IDs-only graph_retrieve + token-heavy neighbors.** Fixed via `docs/superpowers/plans/2026-07-13-daab-retrieval-token-efficiency.md`, executed end-to-end with superpowers:subagent-driven-development (ledger `.superpowers/sdd/daab-retrieval-token-efficiency-progress.md`). Part A: `kg_graph_retrieve` gained opt-in `include_snippet` (batch-fetched, null-discriminated, rune-safe-truncated chunk content inlined on each result) + unconditional `title` on entity_neighbors. Part B: `kg_get_neighbors` gained opt-in `view=slim` (frozen 9-field projection, ~4x smaller in live smoke: 5109 vs 20447 bytes on a real node) + an `edge_id` paging tie-break fix. All 7 tasks TDD'd, task-reviewed (2 fix loops resolved), live-smoked (view=slim path only — include_snippet blocked by no embedding creds in this sandbox, covered instead by reviewed unit/integration tests), and passed 2 independent final whole-plan reviews with 0 Critical/Important findings. Both features opt-in, default behavior byte-identical, zero new MCP tools (bridge invariant held at 47 schemas).

   Original finding: `kg_graph_retrieve` returns bare `chunk_id`/`document_id` (no snippet) → every grounded citation needs a follow-up `kg_search_chunks`. `kg_get_neighbors` returns full node props → **118 KB for 30 neighbors**. Fix = `include_snippet` flag on graph_retrieve + `fields=titles`/ids-only mode on neighbors.
4. **[MED] Uneven extraction across duplicate ingests** — some duplicate copies have 0 extracted entities → `related`/`shared-entities` empty for them; a consumer seeding from the "wrong" copy gets nothing. Largely subsumed by fixing dedup (#1).
5. **[RESOLVED 2026-07-15 — genuinely, on the retrieval-visible path] OCR / `needs_review`** — dropped digits blocked reconfirming the "33,6 ha" phase-2 figure (Q10); "FS" naming collision (financial statement vs feasibility study) is a separate, unrelated naming issue not addressed by this item.

   B2 plan (`docs/superpowers/plans/2026-07-14-daab-ocr-figure-fidelity.md`) shipped all 5 original tasks, individually reviewed clean, plus 2 independent whole-branch final reviews. The second final review found the plan's core goal was NOT achieved on the path that matters (see history below, kept for the record) — this was closed by two follow-up tasks (6 and 7) plus one follow-up fix, each independently reviewed:

   - **Task 6**: wired the existing `_repair_confusables_near_units` (Task 3, RapidOCR-fields path) into the Tesseract body-text/chunking path too (`_extract_pdf` in `files.py`). Fixed the unambiguous `I`/`l`/`O`/`o` confusable class on chunk content, but did NOT fix the two target figures (one hit a regex-scope gap; the other exposed that `§` is genuinely ambiguous — a single fixed mapping can't be correct for all its real occurrences).
   - **Task 7 (the real fix)**: in `worker.py`'s `handle_extract_upload`, appends the RapidOCR-fields path's already-correct structured field values (`structured_fields.areas`, etc.) as a `## Recovered Figures (OCR fields)` markdown section to `content_raw` BEFORE chunking — so values RapidOCR already reads correctly become part of the actual searched/indexed `document_chunk.content`, not just a write-only metadata field. Live-verified via direct DB query: `98,18ha` and `4,38ha` are now literal substrings of the re-ingested `11381263.pdf`'s chunk content.
   - **Follow-up fix**: a whole-branch review of Tasks 6+7 together found Task 6's inherited `§→5` mapping was worse than a no-op — it manufactured a confidently WRONG figure (`"4.3§ ha"` → `"4.35 ha"`, true value `"4,38 ha"`) newly visible in searchable chunk content (pre-Task-6 this only ever reached the unread metadata field). Fixed by removing `§` from the confusable map — it now passes through untranslated (honest signal of a bad OCR read) rather than guessing. Live re-verified: the wrong `"4.35 ha"` is gone (0 matches), the correct `98,18ha`/`4,38ha` (from Task 7's recovered-fields section) are unaffected, controls (`122,81ha`, `33,6ha`) and an unrelated document unregressed, project doc count round-tripped 77→74→77 cleanly each time.

   **Final honest state**: `document_chunk.content` (the actual search/RAG-visible text) now contains the CORRECT figure values for the golden-set's previously-mangled figures, reachable by full-text search, with no fabricated/wrong values anywhere in that path. The original Tesseract-mangled body-text sentence itself remains visibly garbled where the true digit can't be recovered without visual context (no LLM-vision, by design) — this is honest degradation, not a silent wrong guess, and Task 4's `unrecovered` fail-loud marker + Task 7's recovered-fields section together guarantee the correct value is reachable regardless.

   Full detail, all real numbers, and the intermediate (partially-resolved) history: `docs/superpowers/plans/b2-golden-set.md`. Checkpoints: `mem:checkpoint/subagent-driven-b2-final-2026-07-14` (original 5 tasks + the gap discovery), `mem:checkpoint/subagent-driven-b2-task7-recovered-fields-2026-07-14` (Task 7).

   **What genuinely shipped, beyond the headline fix:** the measurement harness (Task 1, reusable for future OCR work); flag-gated, evidence-adopted preprocessing machinery, kept OFF by an honest A/B decision (Task 2); correctly-scoped confusable-repair on both OCR paths, now excluding the one genuinely-ambiguous glyph (Task 3 + Task 6 + the § fix); a fail-loud `unrecovered` safety net, persisted end-to-end, correctly gated against false positives on born-digital/arealess docs (Task 4).

   **Remaining optional follow-up (not blocking):** Task 3's vi/latin PP-OCR model-conversion half remains spike-gated and deferred (no viable ONNX artifact without conversion) — revisit only if a future corpus reproduces Tesseract-style digit-confusable mangling on the RapidOCR-fields path itself (not observed on this golden set).
6. **[LOW] Read-path 403 not reproduced** this run (the `.mcp.json` key was multi-project, not single-project; a wrong-project override probe returned 200-empty, never 403). Re-test needs an actual single-project key. Fix still = key-creation auto-default (`mem:backlog/daab-agent-context-project-resolution-bug`).
7. **[LOW] Corpus-synthesis (FR-002/003/005)** — `kg_get_context` is "Not found" for this corpus; no themes/summary/community tool. But with Tier-3 retrieval working, a human assembles Tier-4 from cross-doc results → global summarization is scale/convenience, LOWER priority than the data-hygiene items above (weaker evidence for immediate build than it looked when Tier-3 was broken).

## Recommended order
(1) document dedup → (2) ship rebuilt bin/kg-bridge → (3) snippet-inline graph_retrieve + ids-only neighbors. All DAAB-solo, evidence-backed, non-speculative. FR-002/003/005 stays deferred.
