# Backlog — agent_context retention/ranking follow-ups (deferred from Phase-1 keystone)

**Context:** Memory retention + recall ranking shipped 2026-06-28 on `task/implement_docs_sync` (8 commits `58b76af`→`a5a6946`), **merged to main 2026-07-07**. Spec `docs/superpowers/specs/2026-06-28-daab-memory-retention-ranking-design.md`, plan `docs/superpowers/plans/2026-06-28-daab-memory-retention-ranking.md`. These were intentionally deferred (spec §10). See `mem:decisions/daab-rbac-isolation-keystone-gate-verdict` and `mem:decisions/ecosystem-hermes-allocation`.

---

## ⚡ VERIFIED AUDIT 2026-07-07 (from source: `store/agent_context.go`, `service/agent_context_retention.go`, `service/memory_settings.go`)

**Current retention engine:** `RunRetentionSweep` (`store/agent_context.go:289`) = 1 tx, 2 passes. Pass A exact-dedup `PARTITION BY project_id,user_id,scope, md5(lower(btrim(content)))` keep `updated_at DESC`. Pass B growth-bound `PARTITION BY project_id,user_id,scope` keep top `bucket_cap` by `updated_at DESC`. Archive = `is_archived=true` + **DELETE embedding**; `mem_key` rows never touched. Recall (`RecallAgentContext`) filters `is_archived=false`, recency decay on `updated_at` + `HalfLifeHours`.

| # | Item | Verified state | Effort | Risk | Value | Dep |
|---|------|----------------|--------|------|-------|-----|
| 1 | Usage-based decay | `last_recalled_at` (mig 000071) **read/written NOWHERE**; recall+sweep both use `updated_at` | S–M | M | **High** | none |
| 2 | Un-archive + hard-delete TTL | archive drops embedding; **nothing reads `is_archived=true`**; **no TTL** → dead rows grow unbounded | M | M | Med | un-archive needs Python re-embed |
| 3 | Semantic near-dup dedup | dedup is **exact md5 only**; no cosine | L | High | Speculative | embed pipeline + tuning |
| 4 | audit_trail for sweep | sweep **slog-only**, no `audit_trail` write; needs `AuditOperation`/`AuditEntityType` enum extension (mig ~035 pattern) | S–M | Low | Med | enum migration |
| 5 | Capture-contract dedup ownership | **docs-only**; code already behaves correctly (mem_key upsert vs free-form) | XS | None | Med | none |

**Recommended sequence:** ① usage-based decay (+⑤ contract, same code region, near-free) → ② TTL hard-delete (DAAB-solo part; un-archive/recovery later, needs Python) → ④ audit_trail (once a real consumer depends on memory) → ③ keep DEFERRED (YAGNI; cosine false-archive = data loss; only if exact dedup proven insufficient on real corpus).

**✅ ①+⑤ DONE 2026-07-08** on `task/implement_docs_sync` (spec `docs/superpowers/specs/2026-07-07-daab-usage-based-decay-design.md`, plan `docs/.../plans/2026-07-07-daab-usage-based-decay.md`; 2-round adversarial reviewed). Commits `3dde306`→`75efe4b`: T2 Pass B `ORDER BY GREATEST(updated_at, COALESCE(last_recalled_at, updated_at))` (`agent_context.go:345`); T3 `TouchRecalled` (`:391`, last_recalled_at-only, throttle 1h const, is_archived guard) + batched `RecalledTouchWriter` (`service/agent_context_touch.go`, drop-on-full, `[]stoppable` shutdown) + recall handler enqueue top-K (`:215`); T1 keyed-cap-exemption test + ownership doc. Verified: build + service/handler unit `-race` GREEN; integration T2/T4 not independently re-run (dev DB auth mismatch) but were the TDD RED→GREEN core. **NOT yet merged to main** (7 commits ahead). Explicitly rejected & NOT built: recall-ranking change, per-recall goroutine, updated_at bump, config knob, functional index.

**REMAINING open:** ② un-archive+TTL (next DAAB-solo buildable), ④ audit_trail (gate on real consumer), ③ semantic dedup (deferred YAGNI).

---

## Original item detail (spec §10)

1. **Usage-based decay** — column `agent_context.last_recalled_at` ships (migration 000071) but is UNPOPULATED. Populate it on recall + switch decay from age-based (`updated_at`) to disuse-based. Avoids re-doing decay policy when recall noise persists. **[VERIFIED still open — `last_recalled_at` referenced nowhere in non-test go source.]**
2. **Un-archive / recovery + hard-delete TTL** — archival is currently a one-way trapdoor (nothing reads `is_archived=true`; embedding is dropped on archive and must be regenerated by the Python worker on un-archive). Add a recovery query and/or a TTL that hard-deletes long-archived rows. Document the trapdoor until then.
3. **Semantic (near-duplicate) dedup** — current dedup is exact (`md5(lower(btrim(content)))`). Add cosine-threshold dedup over the 384-dim embeddings (needs threshold tuning + embedding-pipeline coupling). Only if exact dedup proves insufficient on real corpus.
4. **audit_trail for the sweep** — sweep currently logs via slog only. Full `audit_trail` records need extending the `AuditOperation`/`AuditEntityType` CHECK enums (migration, pattern of 035) to add an archive op + `agent_context` entity type. CTO wanted this for a shared substrate; deferred as over-proportion for a janitor at v1.
5. **Capture-contract dedup ownership** — assert in writing: capture owns keyed dedup (`mem_key` unique index, in-place upsert); retention owns free-form (mem_key NULL) dedup only. Retention correctness is conditional on a complete capture path (gate-2 silent-amnesia, `mem:decisions/daab-hermes-keystone-verification`).

**Config knobs (system_settings, Go-constant defaults):** `memory.recall_half_life_hours`=720, `memory.bucket_cap`=200, `memory.retention_sweep_interval_seconds`=3600.
