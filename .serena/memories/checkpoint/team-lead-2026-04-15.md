# Checkpoint: team-lead — 2026-04-15

## What was done
- Completed BA-021 Python worker tasks (P5, P6, P7) for Claude OAuth Integration
- 5 commits on branch `feature/ba021-python-oauth-embeddings`, merged to main, pushed
- P5: Bearer token propagation — Go API sends resolved OAuth/API key token, Python forwards it
- P6: Local embedding model — sentence-transformers all-MiniLM-L6-v2, 384-dim L2-normalized vectors
- P7: Embedding API endpoint — POST /api/v1/embeddings for Go to call when provider=local

## Files changed
- Created: embeddings/ package (3 source files), api/embeddings.py, 4 test files
- Modified: ai_client/client.py (default_bearer_token), kg_client/client.py (_auth_header), api/streaming.py (token extraction), config.py, main.py, pyproject.toml

## Current state
- All 42 tests pass (16 new BA-021 + 26 Phase 4), lint clean
- Branch merged to main, pushed to GitHub
- Docker images need rebuild (sentence-transformers added ~500MB to image)
- Go API BA-021 tasks already complete (9/9)
- NextJS BA-021 tasks still TODO (7 tasks)

## Next steps
- Rebuild Docker images with new sentence-transformers dependency
- NextJS team: N4-N10 (OAuth UI, callback pages, BFF endpoints, status components)
- After NextJS: end-to-end OAuth flow testing

## Blockers / Risks
- Docker image size increase (~500MB from torch + sentence-transformers) — consider multi-stage build or separate embedding service
- Model download (~80MB) happens on first embedding request — consider pre-downloading in Dockerfile
