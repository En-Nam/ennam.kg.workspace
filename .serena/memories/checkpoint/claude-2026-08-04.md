# Checkpoint: claude — 2026-08-04

## What was done — playbook management layer SHIPPED (phase 3 Tasks 1+2+3a per `docs/mcp-write-phase3-plan.md`)
Branch **`task/playbook-management`** (off `task/mcp-write-datasource`), 2 commits pushed:

- **`ad81951` Tasks 1+2 — lifecycle + drift:**
  - CRUD/approve API (REST/dashboard only, NOT via MCP): POST/GET/PATCH/DELETE `/data-sources/{id}/playbooks[/{name}]` + `approve`/`disable`/`propose`. Create/update validate against CURRENT schema (`validatePlaybookDefinition`: tables, columns, template expansion via synthetic input) → 400 naming exact step/column. PATCH → back to draft + version bump. Approve = project ADMIN, re-validates, records approved_by/at (migration **000085**). Delete refused if playbook ever executed successfully (disable instead). Mgmt actions audited (`playbook_approve:<name>` …).
  - Stale-on-sync (spec §5.3): `SchemaSyncService.SetPostSyncHook` → `PlaybookService.RevalidateForDataSource` — drifted approved/draft → `stale` + precise reason; recovered schema stays stale (human re-approve).
  - **Root-cause fix in schema sync:** apply was upsert-only → dropped columns/tables became GHOST metadata forever (fooling describe + validation + NL-query schema context). Sync now deletes removed columns (`DeleteColumnsExcept`) and dropped tables (`DeleteTableByName`, FK cascade).
- **`dbec243` Task 3a — deterministic proposer (Rule 5 split, no LLM):**
  - FK-topology + naming classifier: entity → `create_<singular>` single-insert; header-detail (`_items|_lines|_details` + FK) → composite foreach sharing `$id.<header>`; ledger (FK+timestamp+quantity col) NEVER standalone, only `ledger_candidates` for review. Self-consistency under test: every generated definition must pass the manual-creation validation gate.
  - `POST .../playbooks/propose` (developer) → drafts (`created_by=proposer:<actor>`), skips existing names, never auto-approves.

## E2E verified (pharmacy_demo, all live)
- bad-column create → 400 naming it · draft execute → 403 · approve → runs · PATCH → draft v2
- DROP payments.status + sync → `stale` ("step payments: column status does not exist") → execute 409 → restore + sync → STILL stale → re-approve → executes (4 rows)
- Propose on real schema: 5 drafts (create_employee/_product/_refund [detected refunds+refund_items pair]/_store/_transaction), skipped create_customer (exists), ledger candidates inventory_* + prescriptions. **Approved machine-proposed create_employee → executed → EMP-9999 in source DB.**
- 22 test packages green; migration at 000085; stack running latest build.

## Current state
- Full insert-data layer live: describe → insert-row → playbooks (execute qua MCP, 55 tools) + quản trị vòng đời qua REST. Zero-SQL flow: connect → propose → approve → LAAM insert.
- Note: Docker Desktop was down at session start (machine reboot) — restarted; Serena MCP was down most of session (this checkpoint is the backfill).

## Session 2 — Task 4 dashboard UI SHIPPED
- **ennam.kg.next branch `task/playbook-panel`** (pushed): `PlaybookPanel` (status badges draft/approved/stale+reason/disabled, "AI proposed" marker, expandable definition JSON, Approve/Disable, Propose-from-schema với kết quả created/skipped/ledger) + `WriteAccessCard` (allow_writes toggle + whitelist editor) trong trang data-source detail. Types + hooks theo pattern use-data-sources, đi qua BFF `/api/kg`, không route mới. Build pass; lint debt cũ (DataSourceForm/Table) không đụng.
- **ennam.kg.go `df323d7`** (task/playbook-management): PATCH /data-sources/{id} nhận `allow_writes` + `write_tables_whitelist` (trước chỉ set được bằng SQL). Round-trip verified.
- **UI verified live** (login dragoon@exnodes.vn): panel render 7 playbooks đúng badge; bấm Approve create_product từ UI → DB `status=approved, approved_by=dragoon@exnodes.vn` — danh tính web-session chảy đúng qua BFF. Screenshot: playbook-panel-live.png (workspace root, untracked).
- Dashboard container rebuild với UI mới, đang chạy :3500.

## Next steps
- Task 3b: LLM enrichment (side-effect steps từ data patterns — composite drafts hiện ghi rõ "side-effects NOT included"; ledger_candidates là input cho pass này)
- MSSQL writes; mở PR cho cả 2 branch (`task/mcp-write-datasource` → main, rồi `task/playbook-management`)
- Hardening backlog cũ: `mem:backlog/go-nl-query-hardening-followups`, `mem:backlog/mcp-write-phase3-followups`

## Blockers / Risks
- None. z.AI provider hết credit (429) — BytePlus vẫn OK, chỉ ảnh hưởng Task 3b sau này.
