# Checkpoint: claude — 2026-08-03

## Session 1 (morning)
- Committed + pushed `docs/mcp-query-datasource-spec.md` to workspace main (`e1806b8`)
- **Pharmacy demo dataset end-to-end**: source DB `pharmacy_demo` (19 tables, 45 FKs, counts match manifest; expected_findings deliberately NOT loaded), project **Michael Pharmacy Chain** `4ad7a5fa-a036-4f6a-8229-39a20af23e38`, datasource **Pharmacy POS DB** `3c7af733-b069-4654-b98e-beb5afde588c`, extract→sync→generate-kg (19 nodes + 41 edges)

## Session 2 (afternoon) — mcp-query spec IMPLEMENTED + pipeline fixed
Branch **`task/mcp-query-datasource`** in ennam.kg.go, commit `92c62e0`, pushed. All TDD; 5 packages green; deployed to :8082/:8765.

**Bridge (spec §3):** 3 new MCP tools `kg_query_datasource` / `kg_query_datasource_status` / `kg_list_datasources` — routes + schemas + tests; 51 tools total (48 HTTP-proxy); all RouteRead (spec §7.1 decided: Read; long-poll §7.2 deferred). Spec's `docs/deploy-mcp-bridge.md` step skipped — file doesn't exist in repo.

**Pipeline fixes (resolves `mem:backlog/go-nl-query-intent-parse-openai-providers`):**
1. `models.FlexStrings` — tolerant unmarshal for aggregations/group_by/order_by (string OR {function,column,alias}/{column,direction}/{expression}); unrecognized shapes fail loud
2. Prompt: explicit element shapes + "strings only" rule; `stripMarkdownFence`
3. Migration **000080**: `clarification_needed` first-class ai_queries status (was failed+error_message); store `UpdateClarificationNeeded` + `GetLatestClarification`; GET /ai-queries/{id} attaches clarification question+options
4. Retry bug: `retryWithSimplification` got table name as datasource id (`plan.Tables[0]`) — never could succeed; now threads real id
5. Legacy schema context 4000→12000 chars (19-table schema was alphabet-truncated; model literally couldn't see `refunds` — clarification options were all early-alphabet tables)

**E2E verified via MCP JSON-RPC on :8765/mcp:** tools/list=51 with readOnlyHint=true on all 3; list datasources → submit "Top 3 employees by total refunded amount" → poll → **completed, SQL joins refunds×employees, returns Sarah Miller EMP-0006 $3689.32 (matches expected_findings, 7× runner-up)**. Clarification flow live-verified (status + question + options through bridge).

## Current state / notes
- z.AI provider (glm-5.2) returns **429 insufficient balance** — selector falls back to BytePlus gpt-oss-120b (works). Recharge or deprioritize z.AI.
- `KG_EMBEDDING_API_KEY` not set → smart context builder disabled (legacy path used; now capped 12k). Setting it re-enables BA-020 smart context.
- Old `.env` KG_API_KEY works (restored DB). For LAAM: create a key scoped to project 4ad7a5fa… (developer role — POST /ai-queries requires it) instead of sharing the admin key.

## Session 3 (evening) — LAAM E2E VERIFIED IN PRODUCTION CHAT UI
- Ran the flow from LAAM chat (localhost:3100, model dola-seed-2.0-pro, Playwright-driven). Two live failures found + fixed (commit `3de7d83`):
  1. list/submit without project_id → 400 despite schemas advertising default-project fallback → both handlers now fall back to `middleware.GetEffectiveProjectID` (TDD, 2 new handler tests)
  2. model-fabricated data_source_id → FK violation surfaced as 500 → now 400 with recovery instruction ("call kg_list_datasources, do not guess UUIDs")
- Config: LAAM key (label 'LAAM', developer, scoped to pharmacy project) got `default_project_id=4ad7a5fa…` set in DB. User had fixed the key's project scope mid-session (earlier 403 was pre-fix).
- **Final proof: LAAM chat answered "Sarah Miller EMP-0006, 3.689,32" via kg_list_datasources → kg_query_datasource → kg_query_datasource_status — 3 tool calls, matches expected_findings.**
- Also ran all 12 demo_questions.txt via REST: 10/12 answerable (see `mem:backlog/go-nl-query-hardening-followups`); MaxTokens 1024→2048 shipped (`c21e93c`).

## Session 4 — "pending" root-caused → wait_seconds long-poll shipped (`b315980`)
- LAAM reported queries "stuck pending" — server was FINE (all complete in 3–7s); the model polls back-to-back with zero delay, burns LAAM's tool-loop cap on instant pending responses, gives up. = spec §7.2 deferred decision proven necessary.
- Shipped `GET /ai-queries/{id}?wait_seconds=N` (clamp 0–25s, under LAAM's 30s MCP timeout): server-side wait until status leaves pending/running. Bridge schema now says "ALWAYS pass wait_seconds=20". TDD (clamp unit test + bridge schema/route tests).
- Second config gotcha: user switched LAAM to a NEW key "LAAM V2" (admin, scoped) with no default_project_id → argless submit 400'd → model wandered (list projects, kg_search) and burned the cap. Set `default_project_id=4ad7a5fa…` on LAAM V2. **Both LAAM keys now have default project set.**
- **Final LAAM verify:** "cửa hàng nào có tỷ lệ từ chối bảo hiểm cao nhất?" → UnityCare Frisco PH-004, 41/265 = 15,47% + rendered chart — matches expected finding #5, clean 3-tool flow.
- New hardening item observed: one query failed `pq: missing FROM-clause entry for table "r"` — model plan used table alias the SQL generator doesn't declare. Added to follow-ups below.

## Session 5 — key-creation auto-default (`91da949`)
- `CreateKey`: key scoped to exactly ONE project now auto-sets `default_project_id` to it (multi-project stays nil — no guessing). Root cause of both manual SQL fixes this session; partially resolves the key-creation half of `mem:backlog/daab-agent-context-project-resolution-bug`. Behavior change: replaced old `WithoutDefaultProjectID` test (asserted nil-stays-nil) with SingleProjectAutoDefault + MultiProjectNoAutoDefault. Deployed.
- LAAM side needs NO changes for any of this (passthrough auth — all logic server-side).

## Next steps
- Open PR for `task/mcp-query-datasource` (5 commits: 92c62e0, c21e93c, 3de7d83, b315980, 91da949)
- Optionally seed benchmark_questions from expected_findings.csv
- Hardening backlog: GROUP BY auto-fix (Q8), alias-in-plan bug ("r"), kitchen-sink question guidance (Q11)

## Blockers / Risks
- None for the demo path. RBAC keystone gate (cross-platform isolation) still applies when handing keys to LAAM — scope tightly.
