# Checkpoint: daab — 2026-07-01 (Fuzzy Hub Merge)

## What was done
- Implemented full DAAB Fuzzy Hub Merge (review_cleared) plan — all 4 code tasks complete
- Task 1 (Go): Migration 000075 — added `review_cleared` to `merge_suggestions_decision_check` CHECK constraint; reversible down migration with safe UPDATE-before-constraint-swap
- Task 2 (Python): `hub_partition_cli.py` — de-diacritic auto-clear gate; `de_diacritic_base` + `is_typo_pair` + async CLI; 3/3 unit tests PASS
- Task 3 (Go): `ApplyReviewClearedMerges` service method + `HandleApplyReviewCleared` handler + route `POST /api/v1/internal/resolution/apply-review-cleared`; fuzzyHubMaxBlastCeiling=40 routes high-degree canonicals back to needs_review; 3 service tests + 3 handler tests PASS
- Task 4 (Python): `fuzzy_sample_cli.py` — added `--decision` param (default `'suggested'`); backward-compatible; 9/9 tests PASS
- Task 5: Operational runbook is human-gated (partition → precision gate → dry-run manifest → apply → re-embed)
- Final whole-plan review: READY TO MERGE — 0 Critical, 0 Important, 2 Minor

## Files changed
- ennam.kg.go: `db/migrations/000075_*.{up,down}.sql`, `internal/service/apply_suggestions.go`, `internal/service/apply_suggestions_test.go`, `internal/handler/apply_suggestions.go`, `internal/handler/apply_suggestions_test.go`
- ennam.kg.python: `src/ennam_kg/resolution/hub_partition_cli.py`, `tests/resolution/test_hub_partition.py`, `src/ennam_kg/resolution/fuzzy_sample_cli.py`

## Current state
- All code complete and reviewed — READY TO MERGE
- Go range: 607cdfc..dd54dc8
- Python range: 628e385..442c2c6
- Task 5 operational steps remain: human must run partition CLI, adjudicate danger stratum, pass precision gate (LCB ≥ 0.97/0.93), then POST apply-review-cleared

## Next steps
- Human: run `hub_partition_cli --project <PID> --dry-run` then without --dry-run
- Human: review danger stratum (remaining needs_review rows), flip approved ones to review_cleared
- Human: run precision gate (`fuzzy_sample_cli --decision review_cleared`)
- Human: POST /api/v1/internal/resolution/apply-review-cleared dry_run=true, review manifest
- Human: POST apply-review-cleared dry_run=false
- Human: re-embed applied canonicals; verify dup-name count drops

## Blockers / Risks
- fuzzyHubMaxBlastCeiling=40 is a first guess — tune from dry-run manifest's degree distribution
- Precision thresholds (0.97/0.93) tune from first scored run
- Minor: no 403 handler test for authorizeBodyProjectID in HandleApplyReviewCleared (non-blocking)
