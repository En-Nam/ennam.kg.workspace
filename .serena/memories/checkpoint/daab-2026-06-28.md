# Checkpoint: DAAB — 2026-06-28

## What was done
- Implemented the full DAAB Memory Retention + Recall Ranking plan (8 tasks, 9 commits)
- Migration 000071: `agent_context.last_recalled_at TIMESTAMPTZ NULL` (forward-compat, unpopulated v1)
- Recency decay in RRF fusion: `fuseAgentRecall(lists, topK, now, halfLifeHours)` — mem_key rows exempt, zero-now disables decay
- Scope-aware PII filter: `(a.scope <> 'user' OR a.user_id = $N)` for user callers; `a.scope <> 'user'` for anonymous
- Over-fetch: each recall branch fetches `max(TopK, 50)`; `a.mem_key` projected into `SearchResult.MemKey`
- `MemorySettings` service provider: 3 settings via `system_settings` with Go-constant defaults (720h/200/3600s)
- `RunRetentionSweep`: tx with dedup pass + growth-bound pass + embedding drop
- `AgentContextRetentionWorker`: ticker background worker with `sync.Once` Stop() guard
- Wiring in main.go: handler gets `recallSettings`, worker started with `defer Stop()`

## Files changed (ennam.kg.go)
- `db/migrations/000071_agent_context_last_recalled_at.{up,down}.sql`
- `internal/store/search.go` — `SearchResult.MemKey string`
- `internal/store/agent_context.go` — decay, scope filter, over-fetch, scanAgentResults, RunRetentionSweep, archiveByWindow
- `internal/store/agent_context_fusion_test.go` — 3 new decay tests
- `internal/store/agent_context_scope_test.go` — 3 scope/memkey/overfetch integration tests
- `internal/store/agent_context_retention_test.go` — 3 retention integration tests
- `internal/service/memory_settings.go` + `_test.go`
- `internal/service/agent_context_retention.go` + `_test.go`
- `internal/handler/agent_context.go` — recallSettings interface + constructor
- `internal/handler/agent_context_test.go`, `_userscope_test.go`, `_isolation_integration_test.go`, `recall_isolation_test.go`
- `cmd/kg-server/main.go`

## Current state
- All 8 plan tasks complete — Go range: 1f57be3..a5a6946
- `go build ./...` clean
- Unit tests (store, service, handler) PASS with -race
- Integration tests (6 recall/retention store + handler isolation) PASS against live DB port 5433
- Final whole-branch review: READY TO MERGE (0 Critical, 0 Important)

## Next steps
- Create PR / merge to main
- File deferred follow-ups: usage-based decay using `last_recalled_at`; un-archive/recovery + hard-delete TTL; semantic dedup; `audit_trail` enum migration for sweep
- Enable DAAB consumer keys: ensure `default_project_id` is set for AAAA/LAAM keys or recall → empty

## Blockers / Risks
- None blocking merge
- Minor: RowsAffected() error silently discarded in embedding DELETE (cosmetic)
- Minor: sweep interval is a construction-time snapshot (runtime changes need server restart)
