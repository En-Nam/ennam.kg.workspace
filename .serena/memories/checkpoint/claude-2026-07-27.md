# Checkpoint: claude — 2026-07-27

## What was done
- Ran `scripts/bootstrap.ps1`: cloned all 4 sub-repos (go, python, next, requirements) fresh — cloned=4, skipped=0, failed=0
- User added root `.env`; started full stack with `docker compose up -d --build` (all images built from scratch)
- Verified: all 7 services Up (postgres, redis, kg-server, indexer, worker, dashboard, kg-bridge)
- Health checks pass: kg-server `:8082/healthz` 200, indexer `:8081/healthz` 200, dashboard `:3500` 200

## Files changed
- None in git-tracked workspace (sub-repos cloned, `.env` added by user — both git-ignored)

## Current state
- Fresh workspace bootstrap on this machine, full Docker stack running
- Non-default ports from `.env`: kg-server **8082** (not 8080), postgres 5433, redis 6380, kg-bridge 8765
- `.env` configures Supabase login, BytePlus extraction LLM (BA-031), Anthropic key
- Ennam KG MCP unavailable this session (stack was down at boot) — booted from Serena only

## Next steps
- Nothing pending from this session; stack is ready for dev/testing

## Blockers / Risks
- None
