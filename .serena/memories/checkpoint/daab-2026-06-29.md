# Checkpoint: daab — 2026-06-29

## What was done
- Implemented `kg_search_sessions` end-to-end (DAAB session/conversation search) via subagent-driven development: fresh implementer + per-task spec/quality review per task, then 2 distinct final review rounds + a fix wave.
- VN-correct FTS over `thread_messages` (config `simple` + IMMUTABLE `f_unaccent`, diacritic-insensitive), user+project scoped, soft-delete excluded, offset pagination, opaque MCP tool contract.

## Files changed (branch task/implement_docs_sync; HEAD 0e0523a)
- `db/migrations/000072_thread_messages_fts.{up,down}.sql` — generated `search_vector` tsvector + GIN index + `f_unaccent`; up has a deployment-note comment (rewrite/lock cost).
- `internal/store/thread_message.go` — `SearchMessages` + `SessionSearchHit`/`SessionSearchParams`; `internal/store/thread_message_search_test.go` (integration).
- `internal/handler/session_search.go` + `session_search_test.go` (unit) + `session_search_isolation_integration_test.go`; wired in `cmd/kg-server/main.go`.
- `internal/bridge/{schema.go,client.go}` + count bumps in `client_test.go`,`handler_test.go`,`schema_test.go`,`integration_test.go`.

## Plan-conformance audit (final gate)
- Opus task-by-task audit: COMPLETE & CORRECT, 0 conformance gaps, all T1–T5 steps PASS, fresh tests green.
- Found + fixed 1 latent plan-inherited bug (commit `84b1cb2`): handler `next_cursor` used uncapped `limit` while store caps page at 50 → rows skipped for `limit>50`. Now handler caps limit at 50; added `TestSessionSearch_LimitCappedAt50`. FINAL HEAD `84b1cb2`.

## Current state
- Working: `go build ./...` clean; `go test -race ./...` 23/23; integration store 3/3, handler 10/10; live smoke HTTP 200/400, body `project_id` does NOT widen scope.
- Bridge invariant 45 schemas == 42 routes + 3 local (see `mem:` count note in agent auto-memory `bridge-tool-count-drift`).
- Security verified (opus review): scope resolved only from API-key identity; ProjectID middleware uses a separate context key the handler ignores → header override can't widen; SQL fully parameterized; `simple`+`f_unaccent` consistent across to_tsvector/plainto_tsquery/ts_headline. No Critical findings either round.
- Project-isolation tests strengthened so the `project_id` clause is the sole discriminator (AGENTS Rule 9 gap closed).

## Next steps
- PR / merge decision (Phase 7). On deploy, mind the migration lock cost if `thread_messages` is large.
- Follow-ups filed in `mem:backlog/daab-kg-search-sessions-followups` (semantic/hybrid v2, monitoring cross-user scope = LAAM Phase-2 blocker, REST role-400, etc.).
- Update `mem:decisions/ecosystem-hermes-allocation`: DAAB Phase-1 keystone now adds session search.

## Blockers / Risks
- None blocking. Cross-user `monitoring` scope needs a decision record + threat model before LAAM can consume.