# Checkpoint: daab — 2026-06-29

## What was done (3 work items this day)

### 1. Monitoring scope for kg_search_sessions — COMPLETE + merged-to-branch
- admin-only cross-user search; decision+threat model `mem:decisions/monitoring-scope-kg-search-sessions`.
- Migration 000073 kg_audit_log; AuditLogStore; requireMonitoringAccess (admin key OR Phase 3 roleResolver); scope= field; bridge schema.
- **Critical SQL bug found+fixed via integration test:** `$1::text` → `uuid = text` operator error → fixed to `$1::uuid`. Monitoring scope was silently empty before.
- Commit `e9a5877` (go). Tests: 10 unit + 6 integration green.

### 2. BA-033 Slice 2 Gate A — RE-MEASURED on live corpus
- Corpus grew: 31 docs/1 project → 176 docs/2 substantive projects (project 592c7ff7 = 145 docs, still Định An/Trà Vinh domain).
- **Key finding:** concepts NOT deduped — "Hàm Giang" = 80 separate nodes → 0 cross-doc bridges now; 35.2% would bridge if deduped by title.
- BA-031 verified as the correct lever (`resolution/pass2.py` resolves concept nodes). But bridges are dominantly administrative → original usefulness concern STANDS. Gate A = PENDING (needs real fuzzy merge + community-quality trial). Gate B (no consumer) blocks Slice 2 regardless.
- Updated `mem:decisions/ba033-gate-a-measurement` (commit 93cc395, workspace).

### 3. include_archived filter for kg_search_sessions — COMPLETE
- Archived threads were leaking into results (only deleted_at excluded). Added include_archived (default false: exclude).
- Store `SessionSearchParams.IncludeArchived` + inlined `AND t.is_archived = false` (is_archived is NOT NULL → exact); handler field; bridge schema property (whole-params map forwards, no tool-count change).
- **Verified:** is_archived NOT NULL (no NULL-exclusion bug); only 1 caller of SearchMessages.
- BEHAVIOR CHANGE: archived now excluded by default (safe pre-adoption).
- Commit `514da7b` (go). Tests: +2 unit, +2 integration (g/h). Full TestSessionSearch suite (16 tests) green.

## Files changed (item 3)
- `ennam.kg.go/internal/store/thread_message.go`, `internal/handler/session_search.go`, `internal/bridge/schema.go`, `internal/handler/session_search_test.go`, `internal/handler/session_search_isolation_integration_test.go`

## Current state
- Branch `task/implement_docs_sync` — both repos committed + pushed. NOT merged to main (user chose to keep working on branch).
- go: HEAD 514da7b (36+ commits ahead of main, 0 behind). workspace: HEAD c8424dd.
- DAAB Phase-1 keystone (RBAC + retention/ranking + kg_search_sessions + monitoring + archived filter) COMPLETE.
- All tests green; migrations applied to dev DB :5432 (note: Định An corpus lives on :5433/daab-postgres).

## Next steps (kg_search_sessions v2 remaining, see `mem:backlog/daab-kg-search-sessions-followups`)
- Semantic + hybrid RRF (BIG, cross-service — needs design/brainstorm session; embeddings infra in python worker).
- Trigram/CJK; response_blocks indexing (needs migration + write-path); accented snippets; REST role-400 validation.
- BA-033 Gate A: run real BA-031 fuzzy merge on project 592c7ff7 → re-measure → community-quality trial (but Gate B still blocks Slice 2 build).

## Blockers / Risks
- `ennam.kg.next/docs/api-reference.md` /sessions/search body STALE for both `scope` and `include_archived` — doc sync follow-up (ironic on a branch named implement_docs_sync).
- Monitoring-scope decision doc says audit "MUST" write before results; code is best-effort (slog fallback). Minor doc/code mismatch.
