# Checkpoint: daab — 2026-06-30 (continued session)

## What was done

Implemented DAAB Related Documents / Shared Entities (Step 2) via subagent-driven development:

### Task 1 — Store layer (commit efab5d3)
- Added `SharedEntity` and `RelatedDoc` types to `internal/store/graph_retrieve.go`
- SQL CTE `docMentionsIDF` + `SharedDocumentEntities()` + `RelatedDocuments()` methods
- Integration test `related_documents_test.go` with blob-resistance validation
- Concept node seed requires `{"definition": "..."}` property (chk_concept_properties constraint)

### Task 2 — REST handler (commits beacb24, c8e6161)
- `internal/handler/related_documents.go`: two endpoints
  - `GET /api/v1/projects/{projectId}/documents/{document_id}/related`
  - `GET /api/v1/projects/{projectId}/documents/{document_id}/shared-entities`
- Both call `requireProjectAccess()` closing path-project IDOR gap
- `?limit` capped at 200
- Wired in `cmd/kg-server/main.go`

### Task 3 — MCP bridge (commit 9fe8bdc)
- `kg_related_documents` and `kg_document_shared_entities` MCP tools
- Schema added to `schema.go`, test counts bumped in handler_test/client_test/schema_test/integration_test

### Fix commit (46afe08) — resolved all reviewer-found critical bugs
- C1: changed handler from `{documentId}` → `{document_id}` (snake_case)
- C2: added `PathParams: []string{"projectId","document_id"}` to both bridge routes
- I3: capped `?limit` at 200
- I4: `out := []T{}` empty slice init instead of nil

## Files changed (ennam.kg.go)
- `internal/store/graph_retrieve.go` — new types + methods + empty slice init
- `internal/store/related_documents_test.go` — new integration test
- `internal/handler/related_documents.go` — new handler (snake_case route params)
- `internal/handler/related_documents_test.go` — unit tests (snake_case PathValue)
- `cmd/kg-server/main.go` — wiring
- `internal/bridge/client.go` — new routes with PathParams
- `internal/bridge/schema.go` — two new schemas
- `internal/bridge/handler_test.go` — count 45→47
- `internal/bridge/client_test.go` — count 42→44
- `internal/bridge/schema_test.go` — count 45→47
- `internal/bridge/integration_test.go` — new tool names in allExpected

## Current state
- All tests PASS (bridge, handler, store packages)
- Final whole-branch review: **APPROVED**
- Bridge count invariant: 47 schemas = 44 routes + 3 local
- Branch: `task/implement_docs_sync` in `ennam.kg.go`

## Next steps
- Merge branch to main (or as per release process)
- Optional: add limit-cap boundary test (limit=250 → clamped 200) — Minor finding from final reviewer
- Consider implementing remaining DAAB Step 2 plan items if any

## Blockers / Risks
- None
