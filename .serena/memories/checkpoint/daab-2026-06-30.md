# Checkpoint: daab — 2026-06-30 (Fuzzy Alias Merge B)

## What was done
- Task 1 COMPLETE: `fuzzy_sample_cli.py` + `test_fuzzy_sample.py` implemented via TDD
  - `is_danger_pair(name_a, name_b) -> bool`: cardinality-guarded (|len(ta)-len(tb)|≤1 AND sym_diff non-empty), number-set diff check, recall-oriented
  - `wilson_lcb(successes, n, z=1.96) -> float`: correct Wilson score lower bound
  - CLI `--draw OUT_CSV` and `--score ADJUDICATED_CSV` modes
  - asyncpg driver, `KG_DATABASE_URL`, `$1::uuid` placeholder
- Fix wave: I1 (cardinality guard) + I2 (GTVT abbreviation test) applied after Opus final review
- Python commits: 9c72e5d → 2f41f6c (feat) → b9c7ca4 (fix)
- 9/9 fuzzy tests PASS, 137/137 resolution suite PASS

- Task 2 Steps 1-3 COMPLETE (operational, read-only):
  - Step 0 verify: all_suggested=fuzzy=4695 ✅; all reasons = LLM natural-language verdicts ✅
  - Manifest snapshot: 4695 rows at /tmp/fuzzy_merge_manifest_2026-06-30.csv
  - Sample drawn: 200 rows at `ennam.kg.python/fuzzy_sample_2026-06-30.csv`
  - band=4695, danger=4259 (90.7%), safe=436

## Files changed
- `ennam.kg.python/src/ennam_kg/resolution/fuzzy_sample_cli.py` (new)
- `ennam.kg.python/tests/resolution/test_fuzzy_sample.py` (new)
- `/tmp/fuzzy_merge_manifest_2026-06-30.csv` (pre-apply snapshot, 4695 rows)
- `ennam.kg.python/fuzzy_sample_2026-06-30.csv` (200-row adjudication sample)

## Current state
- Code: READY TO MERGE (final re-review Sonnet: 0 Critical, 0 Important)
- Runbook: Steps 1-3 done; BLOCKED at Step 4 (human gate)
- danger ratio 90.7% is high but expected for Vietnamese corpus (abbrev↔full dominate); cardinality fix means re-drawing would reduce this — but current sample is valid to adjudicate

## Next steps (human required)
1. **HUMAN GATE — Step 4**: Open `ennam.kg.python/fuzzy_sample_2026-06-30.csv`, fill `same_entity` column (1=same entity, 0=different). ~200 rows. Pay heavy attention to `targeted` stratum (cross-place/number diffs).
2. **Step 5**: `KG_DATABASE_URL=... uv run python -m ennam_kg.resolution.fuzzy_sample_cli --project 592c7ff7-9f6f-4cc5-9094-d9b3b685277e --score ennam.kg.python/fuzzy_sample_2026-06-30.csv` → PASS iff random LCB ≥ 0.95 AND targeted LCB ≥ 0.90
3. **HUMAN GATE — Step 6**: After PASS, explicitly authorize: `curl -XPOST $KG_API_URL/api/v1/internal/resolution/apply -H "Authorization: Bearer $KG_API_KEY" -H 'Content-Type: application/json' -d '{"project_id":"592c7ff7-9f6f-4cc5-9094-d9b3b685277e"}'`
4. Steps 7-8: post-apply verify (dup count drop, re-sample 50, Step-2 spot-check) + Serena checkpoint

## Blockers / Risks
- If targeted LCB < 0.90 after Step 4: build pre-filter excluding is_danger_pair rows from this apply run, re-gate remainder (spec §6)
- Apply mutates shared graph (AAA/LAAM); reversible via manifest + unmerge path
- After apply: trigger re-embed (re_embed_pending set by merge.go); Step-2 IDF self-updates at query time

## Minor findings (non-blocking, roll-up for future cleanup)
- Double `is_danger_pair` call in `_draw` (efficiency nit)
- Silent unknown-stratum discard in `_score` (safe for one-shot CLI)
- Backslash line continuation (style)
- `_score` bare KeyError on unknown stratum value
