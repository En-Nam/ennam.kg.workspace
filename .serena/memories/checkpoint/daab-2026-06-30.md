# Checkpoint: daab — 2026-06-30

## What was done
- **Completed Step 1a** (BA-031 entity resolution re-run on big corpus `592c7ff7…`). Full detail in `mem:backlog/daab-entity-resolution-corpus-rerun`.
- Root cause: corpus ingested via `extract_upload` (no resolution chain); re-triggered all 113 docs / 626 chunks via `POST /api/v1/ingestion/documents/{docId}/extract`.
- Result: 626/626 resolved, 5,686 merges applied, duplicates **876→523** (remainder = hubs → 1b), cross-doc consolidation verified ("Chính phủ" → 1 canonical, 24 edges).
- **Perf fixes (uncommitted):** N extraction consumers via `worker_concurrency` (was singular); conditional gleaning (was unconditional → halved LLM calls). ~×8 throughput overall (5.5h → ~35min).
- Re-triggered 2 tail docs orphaned by mid-run worker rebuild.

## Files changed (UNCOMMITTED, branch task/implement_docs_sync)
- `ennam.kg.python/src/ennam_kg/worker.py` — extraction_consumers list (N from worker_concurrency)
- `ennam.kg.python/src/ennam_kg/extraction/pass1.py` — conditional gleaning (density/len gate)
- `ennam.kg.python/src/ennam_kg/config.py` — `extraction_glean_min_chunk_len=400`, `extraction_glean_density_threshold=2.0`
- `ennam.kg.python/src/ennam_kg/resolution/deps_factory.py` — wire glean settings into Pass1Deps
- `ennam.kg.python/tests/test_ba031a_worker_extraction_consumer.py` — assert ==1 → >=1
- `docker-compose.yml` — `WORKER_CONCURRENCY: ${WORKER_CONCURRENCY:-4}`

## Current state
- Working: backfill 100% done, resolution pipeline verified end-to-end. 7/7 unit tests pass on changed files.
- Uncommitted: the 6 files above. NOT yet committed/reviewed.
- Daab docker stack running (daab-server :8082, daab-worker concurrency=4, daab-postgres, daab-redis).

## Next steps
- Commit the perf fixes (run full `uv run pytest` first; consider code-review per Phase 6).
- **1b**: hub exact-name merge (661 exact-name `suggested` rows pending) — needs own spec/plan.
- **Step 2**: thin related-docs / shared-entities retrieval (reuses neighbors/graph_retrieve stores).

## Blockers / Risks
- Perf changes unreviewed/uncommitted. `WORKER_CONCURRENCY=4` is fine locally but on cloud must = vCPU (cross-encoder is CPU-heavy, ~8 cores at burst).
- 53 tail chunks were re-extracted → may have created transient duplicate entities; pass2 exact-name merge should have absorbed them but not separately audited.
- Don't rebuild worker while a backfill is draining (kills in-flight jobs → stuck chunks).
