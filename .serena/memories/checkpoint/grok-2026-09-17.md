# Checkpoint: grok — 2026-09-17

## What was done
- Read `AAAA_introduce` (AM AI Agent landing) end to end.
- Added Jira/Trello-style card FLIP on the project board panel (`ProjectBoard.tsx`).
  Auto-play when in view: Aurora Diligence→Terms (after bar fills to 100), then Kestrel Approach→NDA (bar to 52), then reset. GSAP Flip, one card at a time, never 3 cards in a column.
- Reduced motion: original still frame, no loop.

## Files changed
- `AAAA_introduce/src/components/panels/ProjectBoard.tsx` only.

## Follow-up (same day)
- Board height jerk: Flip `absolute` pulled cards out of flow; `min-h-[7.25rem]` was shorter than two real cards so columns collapsed. Lanes are now a fixed `h-[8.5rem] grid-rows-2`. Flip uses `simple: true` (x/y only, no width/height morph). Reset is two reverse moves, not a bulk teleport.
- Verified 16 samples / 6s: board height locks at 188px through every move; section height constant 877.8. Initial +2px is font settle before the first move.

## Follow-up 2 (card flight)
- GSAP Flip was a no-op in React StrictMode (timeline killed the same frame) so cards glowed in place. Replaced with invert-FLIP: snapshot rects, gsap x/y on the `li`, arc/scale/glow on `[data-face]`.
- Neighbours slide first (0.16s), then the card flies 0.84s with lift/rotate/signal glow and a `back.out` land.
- Verified: Aurora x 1101→1250 across Diligence→Terms; board height locked 188px; entry gzip 127 kB (Flip plugin gone).

## Current state
- `npm run lint` 1 inherited warning (`useMediaQuery`). `npm run build` green. Entry gzip 136 kB (Flip in entry, still <150). deal-scene still lazy.
- Browser-verified at 1440 EN: Aurora then Kestrel change columns; reduced-motion board stays at seed layout. No page errors.

## Next steps
- User may want drag-and-drop instead of (or as well as) auto-play.
- Commit still user's call.

## Blockers / Risks
- None. Playwright MCP extension was down; verified via headless Chrome + puppeteer-core.
