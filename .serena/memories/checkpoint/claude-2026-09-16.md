# Checkpoint: claude — 2026-09-16

## What was done
- Fixed 4 LAAM chat-quality bugs (gpt-oss-120b via BytePlus): JSON tool-artifact
  leaking into persisted answers; `kg_query_datasource` looping on invented
  `data_source_id` placeholders; DAAB `kg_get_master_record` tool description
  not steering the model to structured fields (ownership/shareholders) over
  full-text search; multi-year financial data from mismatched-scale source
  documents charted as one continuous trend without surfacing the caveat.
- Added SSE progress broadcasting for AAAA sync (per-doc + terminal event) so
  the dashboard's sync dialog can show live progress / detect a stuck sync.
- Fixed AAAA worker bug: pipeline failures were marked `state="ingested"`
  (never retried again) instead of `"failed"`.
- Root-caused and fixed 4 compounding xlsx-ingestion defects (Halcom PM3
  sample docs) that produced hallucinated financial figures: openpyxl
  used-range padding inflating extracted JSON ~100x; a redundant 8000-char
  truncation dropping most of a financial statement before the model saw
  it; extraction `max_tokens=1500` too tight for long Vietnamese entity
  lists; the "regenerate" ingestion path never refreshing a hub node's own
  properties after the first ingest. Added `financial_statement.py`:
  deterministic (non-LLM) Circular-200 Balance Sheet/P&L line-item + unit +
  period extraction.
- Added a number-formatting instruction (thousand separators + explicit
  currency unit) to LAAM's chat system prompt.
- Manually backfilled one already-ingested node (CWP_Financial Statements)
  with the corrected properties, since the regenerate-path fix only applies
  to future content changes.

## Files changed
- `ennam.kg.go`: internal/bridge/schema.go, internal/handler/{aaaa_synced_document,source_connection}.go(+test), internal/store/aaaa_synced_document.go, cmd/kg-server/main.go — committed as 2 commits (e1eaea1, 16dc548) on `task/implement_docs_sync`.
- `ennam.kg.python`: src/ennam_kg/worker.py, src/ennam_kg/ingestion/{adapters/files.py, pipeline/{nodes,extract,engine}.py}, new pipeline/financial_statement.py + tests — committed as 2 commits (adae042, 6966d4b) on `task/implement_docs_sync`.
- `LAAM`: src/app/api/chat/route.ts, src/lib/connectors/index.ts(+test), src/lib/agent/context.ts, new src/lib/chat/strip-tool-artifacts.ts(+test) — committed as 3 commits (3422f8a, 585e340, 0020fc2) on `task/improve-workflow`.
- None pushed to remote — all local commits only.

## Current state
- All 3 sub-repos: working tree clean except LAAM's pre-existing unrelated
  `docker-compose.yml` diff (Tailscale DNS mapping, not from this session —
  left untouched).
- daab-worker/daab-bridge/laam-app images rebuilt and redeployed with fixes;
  containers healthy at session end.
- Test suites: ennam.kg.go bridge tests pass; ennam.kg.python 777/778 pass
  (1 pre-existing unrelated failure in tests/extraction/test_parser.py,
  confirmed present on clean tree before any of today's changes); LAAM
  affected suites pass (context.test.ts 24/24, connectors 26/26).

## Next steps
- User said current answer quality is "tương đối ổn" (reasonably good) —
  no further chat-quality fixes requested right now.
- If more xlsx-shaped docs surface non-Circular-200 statement layouts,
  `financial_statement.py`'s header detector will safely no-op for them
  (returns None) — extend ITEM_LABELS/CODE_LABELS/AMOUNT_LABELS with
  evidence from a real sample before trusting it on Vietnamese-labeled
  statements (currently only English Circular-200 labels are recognized).
- Regenerate-path hub-property refresh (engine.py) only self-heals a
  document the NEXT time its content actually changes at the source; other
  previously-ingested docs besides CWP_Financial Statements were not
  manually backfilled.

## Blockers / Risks
- None open. AAAA source server (aaaa.ennam.vn) was down for part of this
  session (503) but recovered; not a code issue.
