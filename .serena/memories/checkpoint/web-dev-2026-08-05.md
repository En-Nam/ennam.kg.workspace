# Checkpoint: web-dev — 2026-08-05

## What was done
- Chat model default is now cloud-first: `GET /api/chat/info` returns `gpt-oss-120b` (BYTEPLUS_MODELS[0]) when the server holds `BYTEPLUS_API_KEY`; otherwise `DEFAULT_CHAT_MODEL ?? gemma4:e4b`. Mirrors `resolveInternalModel()` precedence in `src/lib/llm/internal.ts`.
- Moved the model constant into the handler so env is read at REQUEST time (matches the existing key-gating pattern; import-time const was untestable via `vi.stubEnv`).
- Empty-state copy: added `chat.emptyCloud` (vi/en/zh) — a billed API model (Claude or BytePlus) must not be described as "running locally". ChatClient picks the key via `isCurrentCloud`.
- Tests: BytePlus-key case asserts the LITERAL `"gpt-oss-120b"` (not `BYTEPLUS_MODELS[0]`) so reordering the whitelist can't silently move the chat default; plus an explicit no-key case.
- Docs: `.env.example` DEFAULT_CHAT_MODEL comment + CHANGELOG `[Unreleased]`.

## Files changed
- `src/app/api/chat/info/route.ts`, `src/app/api/chat/info/route.test.ts`
- `src/i18n/dictionaries/chat.ts`, `src/components/chat/ChatClient.tsx`
- `.env.example`, `CHANGELOG.md`

## Current state
- `npx tsc --noEmit` clean; chat + i18n suites green (32 + 46 tests). Not verified in the running browser at :3100.
- Blast radius: `/constellation` and `/constellation-v2` also read `/api/chat/info`, so their default flips to BytePlus too.

## Next steps
- Visual check on `/chat` (picker preselects gpt-oss-120b under the "BytePlus API — billed" optgroup, once, not under Local).

## Blockers / Risks
- Known tradeoff: BytePlus now wins over `DEFAULT_CHAT_MODEL`, so an operator cannot keep the BytePlus picker while forcing a local default. No new override env var added (AGENTS Rule 2).
