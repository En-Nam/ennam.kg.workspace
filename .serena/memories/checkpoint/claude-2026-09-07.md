# Checkpoint: claude — 2026-09-07

## Session theme: same "read-side built, write-side never wired" bug pattern found 2x + admin UX cleanup

## Round 1 — /activity feed/stats (ennam.kg.go)
- Contract mismatch: backend returned `{"entries":...}` + wrapped/metric-major `{"stats":{...}}`; frontend expected `{"items":...}` + unwrapped period-major stats. Fixed in `internal/handler/activity.go` + `internal/store/audit.go` (added `summarizeActivity()`, `ProjectName` join, restructured `ActivityStats`).
- Deeper bug: `store.AuditStore.RecordAudit` had zero callers outside `service/settings.go` (setting.update) — `audit_trail` had 0 rows despite 11,763 real knowledge_nodes. Wired `RecordAudit` into `NodeService.StoreNode` (node.create), `AIQueryHandler.SubmitQuery` (query.submit), `SchemaSyncService.runSyncSchema` (sync.complete, skip dry-run) via existing `Set*`/`With*` optional pattern. `cmd/kg-server/main.go`: moved `auditStore` creation earlier, wired 3 call sites.
- Commit: `577fb78`.

## Round 2 — /activity project filter (ennam.kg.go)
- `HandleGetActivityFeed` never read the `project_id` query param at all — dropdown filter on the frontend had zero effect server-side. Fixed: validate against `identity.ProjectIDs` (or unrestricted admin) via `slices.Contains`, narrow `filter.ProjectIDs`.
- Commit: same activity.go file, folded into a subsequent commit (check `git log --oneline -- internal/handler/activity.go` if exact hash needed — was verified live with 3 different project_id values before commit).

## Round 3 — LAAM/MCP → DAAB activity verification (no code change)
- Confirmed kg-bridge is a thin HTTP client (`internal/bridge/client.go` `toolRoutes` map) — `kg_query_datasource` → `POST /api/v1/ai-queries`, `kg_store_*` → `POST /api/v1/nodes`, both hit the exact same instrumented code as dashboard-driven activity. `kg_remember`/`kg_recall`/`kg_insert_datasource_row`/`kg_execute_playbook` do NOT go through node/query service — not instrumented (deliberately deferred).
- Ran a real `kg_query_datasource` call via this session's own DAAB MCP connection and left it in place (not test pollution — user asked to see it live).

## Round 4 — /admin/usage rate-limit gauge (ennam.kg.next)
- Gauge derived `provider_id` from `useUsageBudget()[0]?.provider_type` — wrong value (string, not UUID) AND `ai_budget_tracking` is legitimately always empty (all 4 configured providers are free/self-hosted, cost=0 — not a bug). Added `useAIProviders()` hook, page now sources the real provider UUID directly.
- Commit: `e3b3ab5`.

## Round 5 — /admin/usage stat cards (ennam.kg.go) — same anti-pattern as Round 1
- `UsageMetricsStore.Increment()` (writes `usage_metrics`, the table `/api/v1/usage/dashboard` reads) had ZERO callers anywhere, despite `ai_usage_logs` having 8k+ real rows (a DIFFERENT, always-written raw log table). Wired `Increment` into `internal/ai/selector.go` (`SetMetricsStore`/`recordMetrics`, called after both `LogUsage` sites in `Send()`) + `cmd/kg-server/main.go`.
- That surfaced a SECOND pre-existing bug (dead code, never exercised): `Increment`'s SQL reused `$1` for both the `period` column and `date_trunc($1,...)`'s field arg → `pq: inconsistent types deduced for parameter $1`. Also a vocabulary mismatch: `usage_metrics.period` CHECK constraint requires `'hourly'/'daily'/'monthly'`, but code used `'hour'/'day'/'month'` (date_trunc's vocabulary). Fixed via new `periodLabel()` mapping in `internal/store/usage_metrics.go`, splitting the SQL into 2 separate params.
- Added `TestPeriodLabel` unit test (pure fn, no DB — same pattern as `TestSummarizeActivity` from round 1, since there's no DB test harness set up this session — see `mem:go-integration-test-dsn-vars`).
- Verified live: 2 real `kg_query_datasource` calls confirmed `usage_metrics` gets rows and the dashboard API returns real data. Left those queries in place (same precedent).
- Commit: `1cf1032`.

## Round 6 — /admin/usage "Custom" tab (ennam.kg.next)
- `TIME_RANGES` had a "Custom" tab mapping to the same `period: 'daily'` as "30 days" with zero date-picker/state behind it — pure dead UI. User confirmed: just remove it (didn't ask to also fix that "7 days"/"30 days" are identical too, or build a real custom range picker — both offered but declined).
- Commit: `3e3522f`.

## Round 7 — /admin/settings AI model assignment UX (ennam.kg.next)
- User asked what the 8 `ai.model.*` settings on `/admin/settings` are and whether they duplicate `/admin/settings/ai-providers`. Explained: ai-providers = catalog (CRUD of providers/models); `ai.model.<function>` settings = IMP-006 per-function routing (which model UUID handles each of 8 pipeline stages: extraction, nl_query_intent, sql_verification, table_filter, kg_implicit_scoring, kg_description, embedding_description, agentic). Confirmed via DB that the shown UUIDs are `ai_models.id` rows.
- User asked to make the UI friendlier. Added `src/components/settings/AIModelAssignmentRow.tsx` — shows the function's human display name (mirrors `internal/models/ai_function.go`'s `AIFunctions` registry, kept in sync manually, no shared schema) + a `<select>` dropdown of active models by provider/display-name/model-id (via `useAICatalog()`) instead of a raw UUID text input. "Auto (priority order)" option sends `""`, matching `SettingsModelResolver`'s existing auto-defer behavior. `SettingsPanel.tsx` special-cases `ai.model.*` keys to this new row; every other setting category (sync/auth/feature_flags/general) unaffected.
- Commit: `075f8e3`.

## Verification discipline (every round)
- `go build ./...` + `go test ./...` (whole repo, not just touched packages) green after every Go edit.
- `tsc --noEmit` + `eslint` on touched files clean after every frontend edit.
- `gofmt -l` clean on every touched Go file.
- kg-server (`daab-server`) uses Air hot-reload on a bind mount — Go edits take effect in ~1-2s automatically (confirmed repeatedly via binary mtime vs source mtime).
- dashboard (`daab-dashboard`) does NOT hot-reload — full production Docker build. Every `ennam.kg.next` change needs `docker compose build dashboard && docker compose up -d dashboard` (from `/Users/danhtrinh/Projects/Exnodes/EnnamKG/ennam.kg.workspace`) before it's visible at localhost:3500. Done after every frontend round this session.

## Current full state (both repos, branch task/implement_docs_sync)
- `ennam.kg.go`: 3 commits this session (`577fb78`, one more for the project_id filter fix folded in — check `git log`, `1cf1032`). None pushed to origin — branch ahead of origin by several commits. Ask before pushing.
- `ennam.kg.next`: 3 commits this session (`e3b3ab5`, `3e3522f`, `075f8e3`). Not pushed either.
- Both repos' working trees are clean as of this checkpoint (confirmed via `git status --short`).
- Dashboard container rebuilt+redeployed 3x this session (rate-limit fix, Custom tab removal, AI model assignment UI) — currently running the latest image with all 3 frontend fixes.

## Next steps / open items
- **Worth proactively auditing**: this exact "reader wired, writer never called" anti-pattern has now been found TWICE (audit_trail/RecordAudit, usage_metrics/Increment) in this codebase. If the user wants a broader health check of admin pages, grep for store methods with zero callers outside their own file+tests, paired with a handler that reads the table they'd write to.
- `ai_budget_tracking` / Budget Progress widget on `/admin/usage`: confirmed NOT a bug — legitimately empty since all 4 current AI providers have cost=0. No action unless a paid provider gets configured.
- Neither repo's session commits have been pushed to the remote — user has not asked to push.
- No known blockers.
