# Checkpoint: web-dev — 2026-05-13

## What was done
- Fixed 4 UI bugs in the Chat feature (SC-8 follow-up):
  1. **Message order**: Added `useMemo(() => [...messages].reverse())` in `ChatMessageList.tsx` — API returns DESC, display needs ASC (oldest at top)
  2. **Auto-scroll**: Added `threadId` prop to reset `prevMessageCountRef` on thread change; scroll uses `'instant'` on first load, `'smooth'` on new messages
  3. **Thread token count**: Created migration 000045 to fix `update_thread_stats_on_message()` trigger — now uses `COALESCE(agent_total_tokens, tokens_input + tokens_output)` to handle agentic queries
  4. **Layout gap/jitter**: Fixed `/chat/page.tsx` container class to match `[threadId]/page.tsx` (`h-[calc(100dvh-56px)] -m-4 md:-m-6 overflow-hidden`)
- Fixed pagination cursor: was using `messages[0].id` (newest in DESC), corrected to `messages[messages.length-1].id` (oldest)
- Rebuilt and restarted dashboard Docker container to pick up changes (production build, no HMR)

## Files changed
- `ennam.kg.next/src/components/chat/ChatMessageList.tsx` — message reversal + scroll improvements + threadId prop
- `ennam.kg.next/src/app/(dashboard)/chat/page.tsx` — layout fix + pagination cursor fix
- `ennam.kg.next/src/app/(dashboard)/chat/[threadId]/page.tsx` — pagination cursor fix + threadId prop
- `ennam.kg.go/db/migrations/000045_fix_thread_token_trigger.up.sql` — new migration
- `ennam.kg.go/db/migrations/000045_fix_thread_token_trigger.down.sql` — new migration

## Current state
- All 4 UI bugs confirmed fixed and verified via Chrome DevTools MCP screenshots
- Docker dashboard container rebuilt and running with new code
- Migration 045 applied to database

## Next steps
- No known blockers; all reported UI bugs for this session are resolved
- Consider creating new thread and sending a query to do end-to-end verification of token counting

## Blockers / Risks
- Dashboard requires full Docker rebuild for every frontend change — no live dev server in Docker
