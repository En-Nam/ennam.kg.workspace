# Checkpoint: web-dev — 2026-05-12

## What was done
- Executed Layer 2 Browser E2E tests (UI-01 through UI-08) using Chrome DevTools MCP
- Discovered critical gap: `useAgenticStream` NOT wired into the chat page
- Documented all findings in `docs/superpowers/test-results/browser-e2e-results.md`
- Confirmed all 5 E2E implementation tasks (tests/e2e/) were completed correctly last session

## Files changed
- `docs/superpowers/test-results/browser-e2e-results.md` — full browser E2E report (created)

## Current state
- **Agentic engine components**: All implemented (Python FSM, Go API handlers, React components)
- **Chat page wiring**: MISSING — `useAgenticStream` not connected to chat page
- **Working**: Login, thread creation/history, BFF proxy, SSE infrastructure, UI resilience
- **Broken**: TierSelector not rendered, old SQL engine handles all chat queries, MSSQL errors
- **Test results**: UI-01 PARTIAL, UI-02 FAIL, UI-03 FAIL, UI-04 FAIL, UI-05 FAIL, UI-06 PARTIAL, UI-07 PASS, UI-08 PARTIAL

## Next steps
- **P0**: Wire `useAgenticStream` into the chat page (`/chat/[threadId]`)
  - Pass `onTierChange` prop to `QueryInputBar`
  - Include `tier` field in query payload
  - Render `AgenticProgress`, `PhaseIndicator`, `ToolCallStepRow` components
- **P1**: Run Layer 1 API smoke tests (`pytest tests/e2e/test_api_smoke.py`)
- **P1**: Run Layer 3 accuracy evaluation (`pytest tests/e2e/test_accuracy.py`)

## Blockers / Risks
- Without chat page wiring, none of the agentic UI tests can pass
- Old SQL engine has a bug (SELECT wildcard without JOIN) causing MSSQL errors on every query
- `INFORMATION_SCHEMA.TABLES` is blocked in C4K Staging MSSQL instance

---

## Wave 1 — web-dev session (append)

### Done
- Created `AgenticStreamPanel.tsx` — renders AgenticProgress, ClarificationPrompt, error and content
- Replaced `useStreamQuery` with `useAgenticStream` in `[threadId]/page.tsx`
- Added `tier` state, query invalidation `useEffect`, `AgenticStreamPanel` wiring
- `QueryInputBar` now receives `threadId`, `tier`, and `onTierChange`
- Fixed `s` dotAll regex flag (ES2017 target): removed flag since `firstLine` is already a single line
- `formatErrorContent()` added to `ChatMessage.tsx` to strip MSSQL DB-Lib noise

### Verification
- `npx tsc --noEmit --skipLibCheck`: 0 errors
- `npm run build`: passes (all routes compiled successfully)

### Next
- Wave 2 test-worker to verify with Chrome DevTools MCP (UI-01 through UI-08 re-run)
