# Checkpoint: planner — 2026-06-05

## What was done
- Reviewed GitHub integration spec; clarified PAT vs OAuth (data model is polymorphic, both fit).
- Added scope note to spec: PAT caption must require `repo, admin:repo_hook`.
- Confirmed spec was NOT yet planned; wrote full implementation plan.
- Surfaced 2 spec-vs-schema conflicts; product owner chose **per-user model + new `github_connections` table** (hướng 2).
- Wrote plan: `docs/superpowers/plans/2026-06-05-github-integration-per-user.md` (18 tasks, TDD, bite-sized).
- Advisor review caught 3 issues + I found a 4th (deeper) blocker. All fixed:
  1. **Path stability (BLOCKER)** — indexer key is `file_path:name:kind` with ABSOLUTE path; random clone temp dirs → duplicate nodes on every push re-index. Added Task 12a (normalize to repo-relative; needs PO sign-off, affects existing data).
  2. **Disconnect orphans webhooks** — fixed Task 5 to delete all user webhooks before token (spec requirement).
  3. **Webhook handler arity** — was 3-arg def vs 4-arg call + malformed interface; unified to `(lookup, tokens, pub, logger)`.
  4. Multi-repo reconcile — verified differ archives only within scanned file set, so repo B won't wipe repo A. OK.

## Files changed
- `docs/superpowers/specs/2026-06-04-github-integration-design.md` (scope note)
- `docs/superpowers/plans/2026-06-05-github-integration-per-user.md` (NEW — the plan)

## Current state
- Plan complete and self-reviewed. NOT yet executed. No code written.
- Key codebase facts confirmed: `crypto.Encrypt(key from KG_ENCRYPTION_KEY)`; `oauth_tokens` is singleton-per-provider (not reused); `source_connections` generic CRUD exists; `queue.IndexMessage` + python `IndexProjectMessage` need repo_url+github_token; worker `handle_message(dict)`; differ archives within scanned files only; extractor stores absolute file_path.

## Next steps
- Get PO sign-off on Task 12a (repo-relative paths — behavior change, existing projects need re-index).
- Execute plan: subagent-driven (recommended) or inline. Phase order: DB → Go client → connection → sources → webhook → Python (12a first!) → NextJS → E2E.

## Blockers / Risks
- Task 12a touches shared Docker-mount indexing path; existing indexed data needs one-time re-index after.
- Webhook E2E needs KG_PUBLIC_URL reachable by GitHub (tunnel) — else verify HMAC via signed curl.
- KG MCP not queried this session (planning/design work, not code context retrieval).
