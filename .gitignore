# --- Sub-repos (cloned by scripts/bootstrap.*) ---
/ennam.kg.go/
/ennam.kg.python/
/ennam.kg.next/
/ennam.kg.requirements/

# --- Secrets / local (NEVER commit) ---
/.env
/SECRET.md
/.claude/settings.local.json

# --- Cache / tool dirs ---
/.ruff_cache/
/.playwright-mcp/

# --- Local auto-deploy log (scripts/deploy-local.sh) ---
/.deploy-local.log

# --- Local GitOps watcher (scripts/watch-main.sh) ---
/.deploy-state/
/.watch-main.log

# --- Scratch screenshots at root (real images go in docs/) ---
/*.png

# stray secrets archive — never commit
env.zip

# --- git research repos ---
/git_research/
/other_projects/

# --- Confidential M&A data — NEVER commit (DAAB: do not store source docs) ---
/doc_pdf_test/
/*.sql
/*-deal-report.md

# --- OS / local tooling junk ---
.DS_Store
/.mcp.json
